
// Fonts are now handled via globals.css using system fonts and Pretendard
// This file is kept empty or can be deleted if preferred.
// For now, keeping it as a placeholder to avoid breaking imports elsewhere if any remain (though I checked).
// Actually, looking at layout.tsx, I already removed the imports.
export { };
